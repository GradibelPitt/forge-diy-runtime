# Forge 制卡工坊

本地浏览器制卡工具。默认目标仓库：`GradibelPitt/forge-diy-runtime`，分支：`main`。

## 启动

```text
cardmaker/
├── macver/
│   ├── 启动制卡器.command       ← macOS 双击这个文件
│   └── program/                ← 程序、资源、独立依赖环境和本地数据
└── windowsver/
    ├── 启动制卡器.bat           ← Windows 双击这个文件
    └── program/
```

需要 Python 3.9 或更新版本。启动器会创建独立虚拟环境并安装 Pillow，然后默认用 Google Chrome 打开界面；没有安装 Chrome 时提示并临时使用系统浏览器。首次安装依赖需要网络；已安装依赖后，本地制卡可离线使用。服务仅监听 `127.0.0.1`。保持启动窗口打开，按 Ctrl+C 停止。

macOS 首次下载后若文件没有执行权限：在 `macver` 目录执行 `chmod +x 启动制卡器.command`。当前交付副本已经设置执行权限。

Windows 未安装 Python 时，启动器会打开 Python 官方下载页；安装时勾选 Add Python to PATH。

## 制作新卡

1. 粘贴 UTF-8 卡牌脚本，或选择 `.txt` 文件。
2. 程序从 `Name:` 读取中文卡名，从 `Colors:` / `ManaCost:` 读取卡牌颜色，并自动判断稀有度。登记卡集默认 PH01，可改选 BT3K 或 TOKEN_HS。
3. 点击“下一步”进入图片页，上传图片或输入图片直链 / 网页 URL。网页会尝试提取分享图及正文图片，由用户选择。
4. 选择是否自动裁剪，检查预览，点击“检查脚本与输出文件”。
5. 选择“仅保存到本地”或“保存并推送 GitHub”。后者先保存本地副本，再展示最新远端提交预览，最后由按钮确认推送。

脚本、图片、输出分为三个页面，可用顶部页签或底部上一步 / 下一步切换，已输入内容保留。检查按钮、保存按钮始终显示在底部；卡牌预览始终显示在编辑区域旁边或上方。

### 卡名、颜色、稀有度

```text
# Rarity: M
Name:星界守望者
ManaCost:3 G U
Types:Legendary Creature Dragon
PT:4/4
K:Flying
K:Vigilance
Oracle:飞行，警戒
```

- 卡名逐字取自 `Name:`，不根据文件名推测、不自行翻译。制作新卡要求内部名称含中文。
- `Colors:` 优先于费用；未声明时分析 `ManaCost:`。单色放到 `white/blue/black/red/green`，多色放到 `multicolor`，无色放到 `colorless`。混色和非瑞克西亚费用也参与分析；起动费用和 Oracle 中的颜色不改变本程序判断的牌张颜色。
- `Colors:` 多色用英文逗号分隔，例如 `Colors:Blue,Red`；同时支持 `Colors:WU`、`Colors:all`、`Colors:Colorless`。无法识别的写法停止处理，避免猜错目录。
- 新卡未声明稀有度时默认 **Common（C，普通）**；`Types:` 包含独立的 `Legendary` 类型词时自动使用 **Mythic（M，神话）**，`Types:` 字段名和所有类型词检测均不区分大小写，例如 `tYpEs:LEGENDARY Creature`。卡名、规则文字和其他字段中的 Legendary 不参与判断。
- 明确的 `# Rarity: C/U/R/M/S/L`、`Rarity:Rare`、`# 稀有度：神话` 等声明优先。未知或冲突的声明仍会报错。下拉框自动显示检测结果；手动修改会写入 `# Rarity:` 注释，选择“恢复自动判断”可移除声明并重新判断。自动判断本身不改写脚本。
- 已有卡未明确声明稀有度时，优先沿用所选卡集表的原稀有度，再使用上述默认规则。修改脚本和替换卡图模式不更新该登记。

### 图片

- 无论是否裁剪，图片始终命名为 `Name字段.artcrop.jpg`，实际转为 RGB JPEG，处理 EXIF 方向和透明背景。
- “图片自动裁剪”开启：裁切为约 1.37:1，可调位置及缩放，最大输出 1370×1000。
- 编辑时预览固定可见：宽屏在右侧显示，窄屏在编辑页上方显示卡名与原画；短窗口自动缩小预览以避开底部保存栏。调整位置与放大滑块时可直接看到结果。
- 关闭：不裁剪、不缩放，保留原图尺寸与比例，仅转换图片格式及 EXIF 方向。
- 原图保存在导出包的 `original/`，不修改上传的源文件。
- 支持 JPEG、PNG、WebP、BMP、GIF、TIFF；GIF 使用第一帧。图片上限 20 MB、3200 万像素。
- URL 抓取仅支持标准 HTTP/HTTPS 公网地址；登录、反爬或依赖 JavaScript 的页面可能无法获取，请改用图片直链或文件上传。

#### 炉石 Wiki 专用抓取

针对 `hearthstone.wiki.gg` 与 `hearthstone.huijiwiki.com`：

- 支持卡牌文章、`File:` / `文件:` 文件页、`index.php?title=…`，以及媒体查看器复制的 `#/media/文件:…` 链接。
- 指定文件时直接导入该原图；卡牌文章优先筛选 `full` / `Art` 原画，候选显示文件名及可用的尺寸。找到原画时不混入卡框和图标。
- 优先通过 MediaWiki `imageinfo` 读取原图 URL，识别文件重定向；两站缩略图链接会转换为原图地址。
- 灰机公开 API 被限制时，包含具体文件名的链接可回退到公开图片 CDN。普通文章若也无法读取，会提示复制原画媒体链接或“原始文件”直链；指定原图失败不会改用其他图片。
- 不读取浏览器 Cookie，不要求额外安装浏览器自动化环境。裁剪开关与最终中文卡名命名逻辑保持一致。

示例：

```text
https://hearthstone.huijiwiki.com/wiki/Card/41081#/media/文件:Earthen_Scales_full.jpg
https://hearthstone.wiki.gg/wiki/Earthen_Scales#/media/File:Earthen_Scales_full.jpg
```

### 登记卡集

| 选择 | 版本文件 | 卡图文件夹 |
| --- | --- | --- |
| PH01 · 炉石传说（默认） | `Placeholder_Set.txt` | `cards/pictures/PH01/` |
| BT3K · 博图三国新篇 | `BoTu_Three_Kingdoms_New_Chapter.txt` | `cards/pictures/BT3K/` |
| TOKEN_HS · 衍生牌 | `Token_HS.txt` | `cards/pictures/TOKEN_HS/` |

版本文件位于 `app/managed/custom/editions/`；图片路径相对于 `app/managed/custom/`。仓库实际代码为 `TOKEN_HS`，不是 `hs_token`。

`Types:` 含独立类型词 `Emblem` 时自动登记到 TOKEN_HS，优先于手动选择；移除该类型后恢复手动选择。字段名及类型词均不区分大小写。卡名、注释、Oracle 正文及 `NonEmblem` 等子串不触发此规则。只有制作新卡会写入版本登记，修改脚本和替换图片保持原登记不变。

### 编号与同名保护

新增登记格式为 `编号 稀有度 中文卡名 @Custom`，画师字段固定默认 `@Custom`。

按 `[cards]` 中所有已有编号的最大数字值加一，追加到该段最末尾、`[tokens]` 前；不回填编号空缺，也不覆盖空编号占位。三个卡集分别计算编号，即使原表乱序也使用最大编号加一，保留旧行顺序。初始离线快照的下一编号为 PH01 206、BT3K 5、TOKEN_HS 11；已有本地缓存可能更新，实际发布以最新远端为准。

普通制卡入口发现同名卡时立即警告并停止，不能通过覆盖选项绕过。推送前再读取最新远端：新卡不覆盖已有脚本、已占用编号或同名孤立图片。发布预览之后若远端前进，非强制更新会拒绝发布。

## 可选衍生物附件

在脚本页勾选“上传衍生物脚本”，才会出现“添加衍生物脚本”。默认不勾选。支持多份附件，取消总开关后，本次保存和推送不包含附件。

- 可粘贴衍生物脚本或导入 `.txt`。脚本标识对应主卡的 `TokenScript$`；主卡只有一个未添加引用时自动填入，也可手动输入。支持衍生物之间的嵌套引用。
- 脚本写入 `app/managed/custom/tokens/脚本标识.txt`。衍生物内部 `Name:` 可以是英文；文件名使用脚本标识，保持 Forge 引用正确。
- 添加衍生物时可再勾选“上传这张衍生物的图片（可选）”，才会展开图片入口。不提供图片也能保存脚本。
- 衍生物图片支持文件、图片 URL 和上述两个 Wiki，独立选择是否裁剪。按仓库衍生物加载规则命名为 `app/managed/custom/tokens/pictures/脚本标识.jpg`，转换为 JPEG，原图备份在本地 `original/tokens/`。
- 衍生物附件不参与卡名版本登记；与把主卡登记到 TOKEN_HS 是两个独立功能。
- 推送时若已有同标识脚本或同名衍生物图片且内容不同，会停止，避免覆盖其他卡正在引用的衍生物。内容完全相同可复用。
- 新卡和修改已有脚本模式均可附带衍生物；替换已有卡图模式只处理主卡图片。

## 只替换已有卡图

1. 切换到“替换已有卡图”。
2. 选择卡集和已有卡牌，或输入完整中文卡名。此模式不需要上传或粘贴脚本。
3. 上传新图片或提供 URL（包括上述 Wiki 原画链接），按需调整裁剪。
4. 点击“检查卡图替换”，然后仅保存到本地，或保存并预览推送。

沿用已有卡名，输出 `app/managed/custom/cards/pictures/所选卡集/卡名.artcrop.jpg`。本地导出只含新 JPEG、输入原图和操作记录；不生成脚本或版本表。推送前将旧卡图保存到本地导出包的 `previous/` 文件夹，并校验其内容。

发布只替换目标卡图，所有卡牌脚本与版本登记保持原样。若旧卡图在预览后被修改、删除，或卡名存在歧义，程序停止推送。原卡没有所选卡集的卡图时会明确提示，此入口只用于替换已有图片。旧版缓存缺少图片索引时需先联网同步一次。

## 只修改已有脚本

1. 切换到“修改已有脚本”。
2. **直接粘贴 / 上传新脚本即可，无需先搜索。** 程序读取 `Name:` 并精确定位已有脚本。可选地搜索卡名并“读取原脚本”再编辑。
3. 检查后保存，或保存并推送。

此模式不需要主卡图片，不生成主卡图片，不新增或修改版本登记。可另外勾选衍生物脚本及其可选图片。若颜色改变，发布时把新脚本放入正确颜色文件夹并删除同名旧脚本位置；原主卡图片与版本表字节保持不变。同名脚本不唯一、目标不存在或远端脚本在保存后变化时停止处理。

## 本地保存与 GitHub 发布

本地保存产生独立目录，不会直接改动运行中的 Forge 安装。新卡目录包含脚本、JPEG、所选卡集的完整版本表、原图及 `cardmaker.json`；脚本修改模式导出替换脚本及操作记录；已勾选的衍生物附件一同导出。请勿把离线生成的所选卡集的完整版本表覆盖到更晚版本的仓库，联网发布功能会重新合并最新版本表。

GitHub 发布需要目标仓库的 Contents 读写权限。支持：

- 在设置中填写 GitHub Token（不存入文件或 localStorage）；
- `GH_TOKEN` / `GITHUB_TOKEN` 环境变量；
- 已安装并登录的 `gh` CLI（也支持 `program/tools/gh` 配合 `program/data/gh` 的独立配置）。

程序不会读取 Codex 连接器凭据；首次使用 GitHub 发布时需配置上述任一种凭据。公开仓库的编号同步、脚本读取和发布预览可无凭据进行，但受 GitHub API 限额约束。

发布使用 GitHub Git Data API，将本次卡牌改动作为一个提交上传，范围严格限定为：

| 模式 | 允许提交的文件 |
| --- | --- |
| 新卡 | 卡牌脚本、中文卡名 JPG、所选卡集的版本登记，以及已选择的衍生物脚本 / 图片 |
| 修改已有脚本 | 本次脚本及已选择的衍生物附件；颜色改变时同时删除旧位置的主卡脚本 |
| 替换已有卡图 | 对应的一个 `卡名.artcrop.jpg` |

JAR、更新器、`app/BUILD-ID.txt`、`release.json`、`app/manifest-critical.sha256` 均保持不变；代码在预览与推送两个阶段检查允许的文件范围。原图备份、旧图备份、凭据和依赖环境只留在本地；不使用强制推送。完成后读取远端提交并验证每个文件的 Git blob SHA，该验证不会写入仓库校验清单。

本程序只操作上述运行仓库，不修改 `GradibelPitt/forge:diy` 或 Java 引擎，不自动重启游戏。要在游戏中使用新发布内容，请按已有 Forge 启动器更新并重启。卡牌基础静态检查不是 Forge 引擎玩法测试。

## 支持范围

当前面向单面卡牌；双面 / 连体等多面脚本会明确停止，避免只有一张图片时产生不完整导入。无中文内部名的新卡需先修改 `Name:`。中文外观名称与英文内部名的映射不由本工具自动推测。

## 开发与验证

两平台 `program/` 中的程序源码相同，只启动器不同。修改时同步两份代码，避免平台行为漂移。

```text
macver/启动制卡器.command --check
cd macver/program
.venv/bin/python -m unittest discover -s tests -v
```

Windows 检查入口：`启动制卡器.bat --check`；在 `windowsver/program` 目录运行测试：`.venv-windows\Scripts\python.exe -m unittest discover -s tests -v`。

界面与本地程序为本项目新实现；资源快照来自本仓库三个版本表和公开脚本 / 图片索引。卡图不随程序示例分发。遵循仓库根目录 `COPYING` 的 GPL-3.0 许可。

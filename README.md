# Forge DIY Runtime

## 当前更新建议

当前运行版本：`20260806-london-mulligan`

- 本版本继续使用 `forge-game.jar` 注入补丁发布引擎更新，不重新打包桌面聚合 JAR；London 调度每次按起始手牌数量重新抓满 7 张，只有最终保留时才按累计调度次数选择牌并依选择顺序置于牌库底。
- 同步当前 PH01 卡牌与简中资源；萨瓦丝女王已移除标准疯魔，改为只有咒语或异能的效应造成弃牌时才可改为免费施放。清理步骤因手牌上限弃牌和将牌弃掉作为费用均不会命中；进场未支付 `{1}{B}` 时立即回手并永久得 +2/+2，强化可重复叠加。
- `-SyncCustom` 会自动携带简中卡牌资源并校验哈希，避免中文客户端回退到内部英文文字；更新后需重启 Forge 才会载入注入补丁、牌脚本和新的翻译表。
- 正常更新会自动拉取 Git payload 并校验清单。只有普通启动入口失败时，才使用强制修复入口。

# Forge DIY Runtime

## 当前更新建议

当前运行版本：`20260803-daily-all-overlay`

- 本版本使用 `forge-game.jar` 注入补丁发布今日引擎更新，不重新打包桌面聚合 JAR；`GameRule` 牌会在起手前移出牌库，并从抽牌、发现、制造、替换、直接打出和复制路径中排除。
- 同步 PH01 #85–#88（萨瓦丝女王、咆哮魔、无界空宇、圣礼骑士），以及咆哮魔选择范围、援军光环检视十张、大地坠饰费用 `{W}{W}`、八张死亡骑士鹏洛客的指挥官资格与简中说明。
- `-SyncCustom` 现在会自动携带简中卡牌资源并校验哈希，避免中文客户端回退到内部英文文字；更新后需重启 Forge 才会载入注入补丁和新的翻译表。
- 正常更新会自动拉取 Git payload 并校验清单。只有普通启动入口失败时，才使用强制修复入口。

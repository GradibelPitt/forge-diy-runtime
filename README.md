# Forge DIY Runtime

## 当前更新建议

当前运行版本：`20260801-upnp-v2-maestra-cost`

- 准备开房的玩家建议通过普通启动器更新。当前 `forge-gui` overlay 增加了 UPnP/IGD v2 路由器兼容支持，用于房主自动建立端口映射。
- 该修复不改变联机协议、序列化事件或加入者行为；仅加入别人房间的玩家无需只为 UPnP 修复更新。
- 本版本同时同步了当前 DIY 卡牌、卡图、版本表和简中资源；使用这些自定义内容联机的参与者仍建议全部更新，以保持资源一致。
- 正常更新会自动拉取 Git payload 并校验清单。只有普通启动入口失败时，才使用强制修复入口。
